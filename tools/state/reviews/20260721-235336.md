# ⑧査読の記録 20260721-235336

査読した範囲: `d16fbe2`..`e8e7f82`（未査読だった 5 コミット）

この回で査読したコミット:

- e8e7f82 docs(⑧): 査読を毎回回さない運用にし、どこまで査読したかを記録するようにした
- c3aaa88 fix(about): 「行」の限界開示に、測った数字を入れて誤りを訂正した (#7)
- f4fe1b9 feat(⑨): 「行」がどれだけを覆っているかを数え、自分の事実誤りを訂正した
- 3f49cb3 feat(⑨): 設問がどこから来たかを洗い出して公開した
- df7cba9 feat(⑨): 照合をありうる回答すべてで検査し、見つかった欠陥を直した

対象ファイル:

- `tools/README.md`
- `tools/agents/EDITOR.md`
- `tools/agents/REVIEW_CHARTER.md`
- `tools/agents/ask_reviewers.py`
- `tools/agents/audit_matching.py`
- `tools/agents/audit_questions.py`
- `tools/agents/audit_rollcall.py`
- `tools/agents/make_review_request.py`
- `tools/agents/verify_content.py`
- `tools/build_shindan.py`
- `tools/build_site.py`
- `tools/fetch_bill_outcomes.py`

| AI | 判定 |
|---|---|
| Gemini | PASS |
| ChatGPT | BLOCK |

## Gemini — PASS

（モデル: gemini-3.5-flash）

判定: PASS

未確認:
- `tools/build_site.py` を実行する際、カレントディレクトリが `tools/` 以外（プロジェクトルートなど）であった場合に、`state/matching_audit.json` などの相対パス解決に失敗して不具合となる可能性（実行環境の前提ディレクトリが `tools/` であるかは未確認です）。
- `fetch_bill_outcomes.py` がスクレイピングする参議院の議案情報ページのHTML構造が、将来的に変更された場合に伴うパースエラーの有無。

## ChatGPT — BLOCK

（モデル: gpt-5.5）

判定: BLOCK

[F] tools/build_shindan.py:189 — 「この`len(POLICY)+1`問」と表示する一方、直後の内訳・全数検査・設問監査は `POLICY` の `len(POLICY)` 問だけを対象にしており、掲載範囲の表記と実データがずれます。

[F] tools/build_site.py:710 — about.html に「これは3つの会期の断面です」と出ますが、この差分内の監査対象会期は `["217", "221"]` の2会期で、本文中にも「掲載している2会期」とあります。掲載範囲の表記と実データがずれます。

[B] tools/agents/make_review_request.py:45 — `EXCLUDE` に `:(exclude)*.html` を常時入れているため、コメントにある「生成物そのものを見せたいときは --paths で明示」に反して、`--paths guide.html` 等でも HTML 差分が除外される可能性があります。査読依頼の生成物が指定どおりになりません。

未確認: `tools/state/matching_audit.json`、`question_audit.json`、`rollcall_audit.json` は差分に含まれていないため、流し込まれる実数値・同一 stance の有無・GitHub 上の JSON リンク到達性は確認できません。外部一次情報リンクの到達性、参議院議案情報から取得した採決方法の正確性、人の承認を経ているかは確認していません。

---

PASS は「異議なし」であって承認ではない。承認は③検証班のCIと、人がマージ（または公開）することの2つだけ。

## 裁定（人が読んだうえでの処理）— 2026-07-21

ChatGPT の BLOCK 3件は**すべて実在の欠陥**だったので、退けずに採用した。
Gemini の「未確認」2件も併せて処理した。

| 指摘 | 判定 | 処理 |
|---|---|---|
| [F] `build_shindan.py` 「この11問」と言いながら内訳は10問 | **正しい** | 第1問（ワンイシュー）の来歴を1項目として追加し、以降の内訳が第2問以降のものであることを明記した |
| [F] `build_site.py` 「3つの会期の断面」と監査の2会期が食い違う | **正しい。しかも指摘より範囲が広かった** | 実際にサイトが載せているのは第217・219・221回の**3会期**で、監査が2会期だった。`guide.html` の脚注も「2会期を併記」と誤記していた（査読者は差分外なので指摘できていない）。第219回の議案明細124件を取得し、監査を3会期に広げて数字を出し直した |
| [B] `make_review_request.py` `*.html` の常時除外が `--paths` を打ち消す | **正しい** | 名指しの指定があるときは生成物を除外しないよう分けた（`exclude_for()`）。`--paths about.html` で差分が出ることを実測 |
| （Gemini・未確認）`build_site.py` を `tools/` 以外から実行するとパス解決に失敗 | 仕様 | 生成スクリプトは `tools/` を作業ディレクトリとする前提（`README.md` の実行例も `cd tools`）。今回は変更しない |
| （Gemini・未確認）参議院ページのHTML構造が変わったときのパース失敗 | 未対応 | `fetch_bill_outcomes.py` は1件の失敗で会期全体を落とさない作りにしてあるが、構造変更の検知は⑥運用班の担当。**確認していないと明記して先に進む** |

**数え直した結果（2会期 → 3会期）**：参院は議決290件中285件（98.3%）が個人別の記録が残る採決、
衆院は432件中6件（1.4%）。設問の母集団は280件。about.html・research.html・guide.html・
shindan.html の数字を差し替えた。

**再発防止**：掲載会期の出どころを `build_party.py` の `sessions_for()` 1か所に定め
（`agents/_sessions.py`）、監査と③検証班がそこから読むようにした。
③は「監査した会期 ≠ 掲載会期」でFAILする。同じ食い違いは次から機械が止める。
