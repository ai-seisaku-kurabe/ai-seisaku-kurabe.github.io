# ⑧査読の記録 20260721-195626

比較元: `758d207^`

対象ファイル:

- `tools/agents/verify_content.py`
- `tools/build_roster.py`
- `tools/build_shindan.py`
- `tools/build_site.py`
- `tools/fetch_session_speeches.py`
- `tools/fetch_speech_list.py`

| AI | 判定 |
|---|---|
| Gemini | BLOCK |
| ChatGPT | BLOCK |
| Groq | エラー |
| Grok | エラー |

## Gemini — BLOCK

（モデル: gemini-3.5-flash）

判定: BLOCK

[B] tools/build_site.py:9 — `roster.json` を相対パス `"roster.json"` で直接開こうとしています。このスクリプトがプロジェクトルート（`tools/` の親ディレクトリ）から実行された場合、ファイルが見つからず例外が握りつぶされ（12行目の `except Exception`）、名簿データが空になります。その結果、本来 HTML に出力されるべき統一会派の説明文（`roster_note()`）が正常に生成されない不具合が生じます。`fetch_session_speeches.py` と同様に `os.path.dirname(__file__)` を用いた絶対パスにする必要があります。

[B] tools/fetch_speech_list.py:53 — `party_of` および `is_caucus_rep` がインポートまたは定義されていないため、実行時に `NameError` が発生してプログラムがクラッシュします（これらの関数は `fetch_session_speeches.py` で定義されていますが、当ファイル内にインポートする記述がありません）。

未確認:
- `gen_seo` モジュールが実行環境に実在するかどうか（`tools/build_site.py` 内でインポートされていますが、変更一覧にないため直接確認できませんでした）。
- `quote` モジュールが実行環境に実在するかどうか（`tools/fetch_session_speeches.py` 内でインポートされていますが、変更一覧にないため直接確認できませんでした）。

[END OF REQUEST]

## ChatGPT — BLOCK

（モデル: gpt-5.5）

判定: BLOCK
[F] tools/fetch_session_speeches.py:64 — `speakerGroup` に「立憲民主」が含まれるだけで全員を「立憲民主党」と判定するため、「立憲民主・社民・無所属」のような統一会派では社民所属議員の発言を立憲民主党の発言として扱う可能性がある。会派名・所属党判定の誤りに当たる。
[F] tools/build_roster.py:60 — `party_of(group)` が「立憲民主・社民・無所属」のような党名を含む統一会派を未判定会派として検出せず、先に一致した「立憲民主党」として扱うため、統一会派内の別党所属議員を正しく roster で補えない。会派名・所属党判定の誤りに当たる。
[P] tools/build_shindan.py:99 — 「政府の趣旨説明（法務委員会・会議録）」を国会発言リンクとして追加しているが、憲法1条は発言の対象を質問側議員に限り、答弁側・政府側を除外するとしているため反する。
[P] tools/build_shindan.py:113 — 政府の趣旨説明を引用的に用いて設問の判定根拠にしているが、憲法1条は発言の対象を質問側議員に限り、答弁側・政府側を除外するとしているため反する。
未確認: 第221回入管法改正の参院記名投票ページにおける各会派の実際の賛否、同法案の趣旨説明原文、`import quote` が参照する `quote.py` の有無、`gen_seo` モジュールの有無。

## Groq — エラー

openai/gpt-oss-120b: HTTP 413: {"error":{"message":"Request too large for model `openai/gpt-oss-120b` in organization `org_01ky0359teez98bm0tgp2drzte` service tier `on_demand` on tokens per minute (TPM): Limit 8000, Requested 18154, please reduce your message size and try again. Need more tokens? Upgrade to Dev Tier today at https://console.groq.com/settings/billing","type":"tokens","code":"rate_limit_exceeded"}}


## Grok — エラー

grok-4: HTTP 403: {"code":"permission-denied","error":"Your newly created team doesn't have any credits or licenses yet. You can purchase those on https://console.x.ai/team/5ab301c5-f524-4cc5-9f65-f777f7ec54ca."}

grok-3: HTTP 403: {"code":"permission-denied","error":"Your newly created team doesn't have any credits or licenses yet. You can purchase those on https://console.x.ai/team/5ab301c5-f524-4cc5-9f65-f777f7ec54ca."}

---

PASS は「異議なし」であって承認ではない。承認は③検証班のCIと、人がマージ（または公開）することの2つだけ。
